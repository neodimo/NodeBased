# Current state — 2026-09-09

## Scope

M0 first executable 2D slice implemented; the requested larger DCC remains partial.
Product authority: `docs/VISION.md`. Architecture: `docs/ARCHITECTURE.md`.

## Evidence

- Linux local offscreen: 19/19 tests passing.
- Headless CLI subprocess: create/view/render/save passed.
- Actual shell screenshot inspected: `artifacts/desktop.png` (deliberate ignored
  local test output, generated by the suite; also configured as a CI artifact).
- Windows/Linux GitHub CI: pending initial push/run; no Windows claim yet.
- Native display/GPU interaction and performance: unverified.
- Delivery: initial implementation being committed/pushed to `main`; verify
  `git status --short` and remote HEAD before reporting delivery.

## Next

Gonzo: finish remote CI verification and update this file.
Omid: run `uv run nodebased` and evaluate graph/viewer/property ergonomics.
Then M1 production 2D substrate (EXR/OCIO, sequences, scheduler/cache/ROI), followed
by native/GPU experiments and lightweight 3D. See milestone acceptance gates.
